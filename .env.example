# .env.example
# The secure API key for server-side Gemini AI model access
GEMINI_API_KEY=

# Secure API token to authorize and authenticate client requests to the Orchestrator API
# Set to any strong token (e.g. alphanumeric string) to protect endpoints.
SWARM_API_TOKEN=
